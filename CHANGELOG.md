# Changelog

本文件记录 VoiceClone 桌面应用的用户可感知变更。发布清单与产物要求见
`docs/release/update-publishing.md`；格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.2.0] - 2026-09-23

### 新增

- **语气/非语言标记按引擎呈现**（issue #68）：每个引擎只显示自己原生支持的正文标记——VoxCPM2 官方 10 个标签（笑叹/呼吸停顿/疑问确认）、MiniMax 19 个语气词 + `<#秒#>` 停顿插入；带搜索的分类下拉 + 常用项快捷按钮；接入单条生成、长文本任务与盲听对比；**不支持的引擎整块隐藏**，不再出现无效选项。
- **Qwen3-TTS 云端指令控制引擎**（qwen3-tts-instruct-flash）：用自然语言指令控制语气、语速与方言（如"用兴奋的语气、稍快的语速说"）。
- **Qwen3-TTS 本地（0.6B Base）参数补全**：新增**语言选择**（真机实测语言 token 确实改变生成）、`max_tokens`（长句不再截断）与 `split_pattern`（显式分段控制）。
- **FireRedTTS3 语种选择**：解除硬编码中文，提供官方 24 语种下拉（默认中文）。
- **界面设计系统收口**（ADR-0013/0014 落地）：**主题三选一**（跟随系统 / 深色 / 浅色，设置持久化），全部颜色/间距/字号令牌化，双主题对比度达 WCAG AA；右侧面板开关移至**顶栏右侧**。

### 修复

- VoxCPM2 控制指令在有转写音色上**被整段念出**：上游禁止控制指令与参考转写并用；现在填了指令会自动切换为参考音频-only 模式（实测指令生效），日志注明降级。
- 文本归一化不再破坏含数字的标记语法（MiniMax 停顿 `<#2.5#>`、IndexTTS 拼音注音 `<汉字|XING2>`）。
- 纯音色设计引擎（Qwen3-TTS 音色设计 · 阿里百炼）不再出现在生成引擎选择器，统一收敛到音色库的音色设计入口。

## [1.1.0] - 2026-09-22

### 新增

- **应用在线更新**（issue #62 / ADR-0020）：
  - 每次启动自动检测 GitHub 最新正式 release（5 秒超时静默放弃，不打扰使用）。
  - 发现新版本时弹窗提醒：版本号 + 更新日志摘要，「去更新」直达设置页，「关闭」，可勾选**跳过此版本**（下一个新版本仍会提醒）。
  - 设置页新增「关于 / 更新」区：当前/最新版本、手动检查更新、更新日志、下载进度、实际生效渠道。
  - 应用内下载安装包（进度可见、可取消），完成后 Windows 自动启动安装器、macOS 自动挂载 dmg 引导安装；下载失败自动回退浏览器打开发布页。
  - **更新渠道**三选一：自动（默认，镜像优先 + 官方静默兜底）/ GitHub 官方 / 国内镜像；镜像前缀可编辑（默认 `https://gh-proxy.com/`），file://、http:// 等不安全前缀被拒绝。
  - 下载完成后按 release 清单做 sha512 完整性校验；清单缺失时警告但允许继续。

### 变更

- 无

## [1.0.0] - 2026-09-22

### 新增

- 首个正式版本：统一界面编排云端 API 与本地模型，完成声音复刻、音色设计与文字转语音。
- 本地引擎：Qwen3-TTS MLX / IndexTTS-2.5（CUDA + MPS）/ VoxCPM2 / dots.tts / FireRedTTS3；多厂商云端引擎按能力矩阵呈现。
- 内嵌 uv 0.12.10（下载经官方 sha256 校验），首次启动自动创建 Python sidecar 运行时。
- 单实例锁 + sidecar 进程组整树终止；SQLite 存储层与一次性迁移。
- 合规护栏、能力矩阵、长文本自动分段与任务队列、音色包导出导入与整库备份恢复。
