# 能力矩阵与听感记录（issue #16，2026-09-18 建档）

> 本文件是 issue #16 的人工执行记录。打勾 ≠ 凭空宣称：每一行结论都要指向
> 回归会话 ID、盲听会话 ID 或一次可复现的命令。

## 1. 五引擎 × 中文回归集

- [x] 回归集固化（17 条，五类难例）并进入 sidecar 与界面
- [ ] 五引擎各跑一遍并记录 —— 执行方式见 `README.md`；完成后在此回填：

| 引擎 | 回归会话 ID | 成功/失败 | RTF 均值 | 备注（哪类难例读错） |
|---|---|---|---|---|
| Qwen3-TTS 0.6B Base（本地 · MLX，Mac 开发机） | `4a4dd3fcd385460b84fa1839b3cddff8`（2026-09-18） | 17 / 0 | 0.979（端到端口径） | 无失败。ASR 可懂度复核未随跑（本地转写未装）；主观听感待 §3 |
| IndexTTS-2.5（本地 · CUDA，RTX 3090） | 待跑 | | | |
| Qwen3-TTS 复刻（阿里百炼 · 云端） | 待跑 | | | |
| Qwen3-TTS 音色设计（阿里百炼 · 云端） | 待跑 | | | |
| Fake（内置，仅验证链路） | 测试套件内验证 | — | — | 合同测试覆盖 |

## 2. 真机性能（RTX 3090 24GB，ALAN-PC）

- [ ] 实测完成 —— 跑回归后把 `peak_vram_bytes` / `rtf` 回填
      `capability_matrix.json` 的 `indextts-25-cuda` → performance →
      `RTX 3090 24GB（本机 ALAN-PC）` 条目，并把验证等级升为 `measured`。

| 指标 | 实测值 | 测量条件 | 日期 |
|---|---|---|---|
| RTF（回归全集均值） | 待测 | 回归集 17 条，参考音色 | |
| RTF（最长条目） | 待测 | | |
| 峰值显存（基线增量） | 待测 | sample_peak_vram，nvidia-smi 200ms 采样 | |
| 常驻显存（闲置 10 分钟后） | 待测 | 对照 issue #649 的 10GB 常驻风险 | |

已知参照（不替代本机实测）：官方 README 4090 RTF 0.2065（verified）；
zeeklog 4080 vLLM 0.15（unverified，维持未核实）；HF 模型卡「约 6GB」（vendor）。

## 3. 真人盲听（主观听感 / MOS）

- [ ] 完成 —— 现状缺口：目前只有 ASR 可懂度复核，**从未做过主观听感**。

核心引擎对（建议至少两组）：

| 对比组 | 盲听会话 ID | 结论（谁更好 / 平手 / 场景差异） | 日期 |
|---|---|---|---|
| Qwen3-TTS-MLX（Mac） vs IndexTTS-2.5（3090） | | | |
| 本地 vs 云端（qwen3-tts-vc-cloud） | | | |

操作：界面「盲听对比」→ 同一音色、回归集里选代表性文本 → 盲标签评分 →
Reveal 后把结论誊进上表。评分明细以盲听会话记录为准，本表只写结论。

## 4. 「未核实」逐条台账

原则：每条要么升级为已核实/实测（附证据），要么明确写成**仍未知**。

| # | 原「未核实」项（出处） | 现状 | 证据 / 结论 |
|---|---|---|---|
| U-1 | IndexTTS-2.5 RTF 0.15（RTX 4080, vLLM）—— zeeklog 仅搜索摘要（引擎选型报告 L865） | **仍未知** | 非本项目环境；4090 官方 0.2065 已是 verified；本机 3090 待测（§2） |
| U-2 | IndexTTS-2.5 在 3090 上的真实 RTF 与峰值显存 | **待实测** | 本文件 §2 回填后升 `measured` |
| U-3 | IndexTTS-2.5 常驻显存（issue #649 报 10GB） | **verified（风险确认）** | 上游 issue 一手引用已录入矩阵；本机复测归 §2 第 4 行 |
| U-4 | 五引擎主观听感 / MOS | **仍未知（现状缺口）** | 本文件 §3；完成前矩阵中 MOS 一律 `unknown` |
| U-5 | Qwen3-TTS 云端计费 / 数据用途 / 禁转售条款 | **已核实** | 阿里云一手条款（issue #9 落地时核过），矩阵 `verified` |
| U-6 | Qwen3-TTS-MLX RTF 0.35–0.40 / 峰值 2.2GB（M5 Pro） | **measured（本机 M5 Pro）** | 2026-09-16 实测补充报告；3090 无关，Mac 侧不重测 |

> 与 `docs/research/` 各报告中模型选型类的「未核实」（未入选产品引擎的
> 候选模型）无关——那些属于调研存档，不在本工单范围。

## 5. 结论固化的位置

- 机器可读：`sidecar/voiceclone_sidecar/data/capability_matrix.json`
- 界面渲染：「能力矩阵」区（`GET /capability-matrix`）
- 回归明细：本地数据根目录 `regression.json`（随整库备份走）
