# 声音复刻软件

一款 macOS / Windows 桌面应用：用统一界面编排**云端 API** 与**本地模型**，完成声音复刻、音色设计与文字转语音。

## Language

### 声音的来源

**音色 (Voice)**:
一个逻辑上的声音身份——名字、头像、文字说明、参考音频与试听样本。它与任何具体引擎无关。
_Avoid_: 音色预设, 音色档案, voice profile, 角色

**声音复刻 (Voice Cloning)**:
从参考音频复制一个已经存在的声音。
_Avoid_: 克隆, voice clone, 复刻

**音色设计 (Voice Design)**:
用文字描述创造一个并不存在的声音，不需要参考音频。
_Avoid_: 文生音色, 造音, TTS 造音

**控制指令 (Control Instruction)**:
写在待合成文本开头的自然语言声音描述（VoxCPM 语法为英文圆括号前缀，如 `(warm female voice)`），用于音色设计与克隆声音的风格控制。它是**引擎语法**，在文本归一化之后拼接，永不经归一化层。
_Avoid_: 风格标签, 情感参数, style prompt

**非语言标签 (Non-verbal Tag)**:
插入正文中间的原生语法标记，让引擎发出非语言声音或停顿。**语法按引擎各异**（VoxCPM 用英文方括号如 `[laughing]`，MiniMax 用中文语气词标签与 `<#秒#>` 停顿标记），标记集由引擎声明；不支持正文的引擎不提供此功能。它是**正文的一部分**，文本归一化与发音标注改写必须原样保留——包括标记内的数字（如停顿秒数、拼音声调）——不得破坏或翻译。
_Avoid_: 语气词, 拟声词, 特效标记

**情感参数 (Emotion Parameter)**:
通过 API 参数（而非正文标记）控制情感表达的引擎机制，如 MiniMax 的 `emotion` 枚举、IndexTTS 的 `emo_vector`/`emo_text`/`emo_audio`、Qwen3 instruct 的 `instructions` 自然语言指令。它与「非语言标签」是**两套不同机制**，不可混称：情感参数是引擎调用面的一部分，非语言标签是正文内容。
_Avoid_: 语气参数, 情感标签（与非语言标签混用）

**发音标注 (Pronunciation Annotation)**:
用户对单个词的显式读音标注（如 `你|ni3`），由规范层按引擎语法改写为音素（中文拼音 `{ni3}`、英文 CMUDict `{W ER1 L D}`）。它与文本归一化的执行顺序是固定的：先归一化、后标注改写。
_Avoid_: 音标, 多音字标注, phoneme tag

**参考音频 (Reference Sample)**:
用于复刻的输入录音。
_Avoid_: 样本音频, 底模音频, prompt audio, 提示音频

### 引擎

**引擎 (Engine)**:
一个能合成语音的具体后端，要么是云端 API，要么是一个本地模型。
_Avoid_: 模型, 供应商, provider, TTS 服务

**引擎能力 (Engine Capability)**:
一个引擎声明自己支持的维度——语种、复刻、音色设计、发音控制、情感、商业许可、跨设备使用、全流程能否由 API 闭环。界面据此启用或禁用功能，而不是让用户撞墙。
_Avoid_: 引擎参数, 模型规格, 能力矩阵

**能力矩阵 (Capability Matrix)**:
issue #16 固化下来的评测矩阵：每个引擎的实测与调研结论，逐格带验证等级（实测 / 已核实 / 厂商口径 / 未核实 / 未知）。它与「引擎能力」不同——引擎能力是运行时声明，决定界面开不开功能；能力矩阵是评测证据，回答「这台机器上它到底行不行」。驱动「能力矩阵」界面区。
_Avoid_: 引擎能力（混用）, 引擎参数, 模型规格, 榜单

**引擎绑定 (Engine Binding)**:
一个音色在某个引擎上的具体实现——云端音色 ID，或本地模型状态与参数。绑定是**可重建的缓存**，不是真相来源。
_Avoid_: 音色 ID, voice_id, 映射, 音色实例

**引擎配置 (Engine Config)**:
一个引擎的连接与安装类设置——密钥、区域与 base URL、量化档、本地权重目录。字段由引擎自行声明，界面按引擎渲染。跨引擎通用的配置（下载源、运行时根、存储位置）**不是**引擎配置，归全局设置。
_Avoid_: 引擎参数, 引擎设置, 模型配置

### 生成

**生成任务 (Generation)**:
一次「文本 × 音色 × 引擎 × 参数 → 音频」的执行记录，含实时日志与产物。
_Avoid_: job, 任务, 请求, 推理

**生成参数 (Generation Parameter)**:
一次生成时可调的参数，随生成任务一起入档，参与盲听对比与偏好画像。与「引擎配置」的分别在于**是否进生成记录**：生成参数进，引擎配置不进。
_Avoid_: 模型参数, 推理参数, 引擎参数

**规范层参数 (Canonical Parameter)**:
值语义跨引擎一致（语速、语种、发音标注等少数几项）的生成参数，在界面作为主层面暴露，且**每个规范参数必须配逐引擎适配器**把「用户看到的值」换算为「引擎收到的值」（如语速 → IndexTTS 的时长倍率取倒数）。其余参数一律留在位置固定、默认折叠的引擎专属层（见 ADR-0018）。
_Avoid_: 通用参数, 共享控制集（不做适配器的共享是静默错误）, 高级参数

**文本归一化 (Text Normalization)**:
所有待合成文本在进入任何引擎之前必经的统一改写——数字、日期、时间、金额、单位、中英混读。它是生成路径的**必经节点**（见 ADR-0008），不是引擎的可选能力；界面在生成前展示其结果。
_Avoid_: 文本预处理, 数字转换, text preprocessing

**重跑 (Rerun)**:
把一条历史生成任务的输入重新载入生成面板，供人工调整后再执行。
_Avoid_: 重试, 复现, 再生成, 重新生成

**分段任务 (Segmented Job)**:
一篇超过引擎单次字符上限的稿件，被切分为多个分段并排队依次生成，完成后拼接为一条完整音频。队列状态可见、可取消；限流由队列与自动重试吸收（见 ADR-0011）。
_Avoid_: 队列任务, batch, 批处理, 切分

**分段 (Segment)**:
分段任务中的一个引擎单次请求及其生成记录。分段按句界切分，且拼接后与原文逐字一致。
_Avoid_: chunk, 块, 片段

**偏好画像 (Preference Profile)**:
盲听对比评分的**只读**汇总——按语种 × 文本类型 × 引擎聚合的平均分与次数。它只回答「到目前为止你更喜欢谁」，不改变默认引擎，也不参与任何自动路由。
_Avoid_: 偏好设置, 用户画像, 推荐引擎

### 运行时与下载

**下载源 (Download Source)**:
安装本地引擎与本地转写工具时获取文件的来源。它分三个**互不合并**的轴：**权重源**（HF 官方 / hf-mirror / ModelScope）、**包索引**（PyPI 及其镜像）、**轮子源**（PyTorch CUDA wheel）。用户选定的是**首选源**，其余源作为静默兜底；界面须显示**本次实际由哪个源服务**。
_Avoid_: 镜像, mirror, 源, 下载地址, 加速源

**转写工具 (Transcription Tool)**:
为本机参考音频转写提供语音识别的**本地工具**（如 mlx-whisper / faster-whisper）。它不是引擎——不能合成语音，也不出现在引擎列表；它的安装、状态与下载进度以正式卡片呈现在引擎页，而「选谁来做转写」（它或某个具备转写能力的引擎）是另一件事，归转写提供方。
_Avoid_: 转写引擎, 本地模型, ASR 服务

**更新渠道 (Update Channel)**:
应用自身获取新版本安装包的来源渠道：**GitHub 官方**或**国内镜像**（可编辑的加速代理前缀）。用户选定的是**首选渠道**，另一渠道保留为静默兜底；界面须显示**本次实际由哪个渠道服务**。它与「下载源」互不相干——下载源服务模型权重，更新渠道服务应用安装包。只认**正式 release**，预发布版不参与自动提醒。
_Avoid_: 下载源（混用）, 镜像源, 通道, 升级渠道

**跳过此版本 (Skip This Version)**:
用户对某一次新版本提醒的免打扰标记：记录被跳过的 release tag，此后仅当检测到的最新版本不是该 tag 时才再次提醒；设置页手动检查不受它影响。
_Avoid_: 忽略更新, 关闭提醒, 禁用更新
